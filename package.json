{
  "name": "dockogram",
  "version": "0.0.1",
  "description": "telegram bot, docker DJ",
  "main": "index.js",
  "scripts": {
    "precommit": "lint-staged",
    "setup": "docker build -t docker-builder -f docker_builder.Dockerfile .",
    "test": "echo 'What about the tests?'; sleep 2; echo 'Nope!';"
  },
  "license": "CC0",
  "dependencies": {
    "dotenv": "^6.0.0",
    "telebot": "^1.2.6",
    "validate-dockerfile": "^1.8.1"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^7.2.0",
    "prettier": "^1.13.7"
  },
  "lint-staged": {
    "*.{js}": ["prettier --single-quote --write", "git add"]
  },
  "prettier": {
    "singleQuote": true
  }
}
